input {
  redis {
    data_type => "list"  
    key => "filebeat_access" 
    batch_count => 100  
    type => "access_log"
  }
}
output {
  if [type] == "access_log" {
  #elasticsearch {
  #  template_overwrite => true
  #  hosts => ["localhost:9200"]
  #  index => "logstash-aptg-access-log-%{+YYYY.MM.dd}"
  #}
  }
  stdout { codec => rubydebug }
}
