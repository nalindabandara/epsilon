 class Program

    {
        public static string UserName;
        public static bool färdig = false;
        public static int UserInput;
        public static int RandomScore;
        static void Main(string[] args)
        {
            while (!färdig)
            {
                Console.WriteLine("Welcome what is your name?");
                UserName = Console.ReadLine().ToLower();
                CheckNoDigits(UserName);
​
            }

            //här skall vi ha kod för att hitta användaren i databasen!!
            #region Meny
            switch (UserInput)
            {
                case 1:
​
                    break;
                case 2:
​
                    break;
                default:
                    break;
            }
            #endregion
        }
        public static void CheckNoDigits(string x)
        {
            foreach (char item in x)
            {
                if (char.IsDigit(item))
                {
                    Console.WriteLine("Only text please :" + item);
                    break;
                }
                else
                {
                    färdig = true;
                }
​
            }
​
        }
        public static void CheckYesDigits(int x)
        {
            foreach (char item in x.ToString())
            {
                if (!char.IsDigit(item))
                {
                    Console.WriteLine("only digits please :" + item);
                    break;
                }
                else
                {
                    färdig = true;
                }
​
            }
​
        }
        public static string CheckDbContext(string User)
        {
            using (GameContext contextp = new GameContext())
            {
                Player p = new Player();
                p = contextp.Player.Find(p.Name);
​
                if (p.Name == UserName)
                {
                    UserName = p.ToString();
                    return UserName;
                }
                else
                {
​
                }
            }
​
​
            return UserName;

        }
    }
}
